# MiniMax H3 Local 导演台使用说明

适用版本：DramaClaw v1.3.2
更新时间：2026-08-15

## 1. 先判断它适不适合你的工作

这套导演台适合把剧本拆成短镜头，管理角色/场景/道具参考图，并用本地 H3 生成
候选视频。最实用的方式不是“一次生成整集”，而是每次做一个 5 秒镜头，挑选后再
续拍、补帧、放大和剪辑。

成功标志：你能把一个场景拆成若干镜头卡，并明确每个镜头的首帧、动作、镜头运动、
声音和结束画面。

## 2. 访问地址

- 导演台：`http://<服务器IP>:8080`
- API：`http://<服务器IP>:8780`
- H3/ComfyUI：`http://<服务器IP>:18189`

成功标志：导演台能打开，在自由画布的视频节点中能看到 `MiniMax H3 Local`。

## 3. 开始前准备四类素材

1. 角色定妆图：正面最重要，可再准备侧面和背面。
2. 场景图：尽量确定空间布局、时间和光线。
3. 道具图：只放剧情中必须保持外观一致的道具。
4. 镜头表：每个镜头写清时长、景别、动作、运镜和声音。

参考图宁少勿杂。多张互相冲突的角色图会降低一致性。

## 4. 推荐的完整流程

```text
剧本
  → 拆成5秒镜头
  → 固定角色/场景/道具
  → TURBO试构图
  → FAST重做入选镜头
  → QUALITY只做关键镜头
  → 上一镜头末帧续拍下一镜头
  → 补帧/放大/配音/字幕/剪辑
```

成功标志：每个最终镜头都能追溯到对应的参考图、提示词和上一版候选，而不是反复
盲抽。

## 5. 如何添加 H3 视频节点

1. 进入自由画布。
2. 添加“视频生成”节点。
3. 在模型选择器中选“本地 / MiniMax H3 Local”。
4. 连接首帧，或切换到参考图模式连接素材。
5. 先选 480P、5 秒、TURBO。

当前开放范围：

- 画质：480P
- 画幅：9:16、16:9、1:1
- 时长：5、8、10、15 秒
- 输入：首帧、首尾帧或 1–9 张参考图

## 6. 三个质量档怎么选

| 档位 | 用途 | 建议占比 |
|---|---|---:|
| TURBO | 构图、动作和镜头方向试跑 | 约 70% |
| FAST | 已入选镜头的正式重做 | 约 25% |
| QUALITY | 少量关键镜头终稿 | 约 5% |

不要把社区 4 步 Turbo 与导演台现有的 `TURBO` 档混淆。导演台的生产 TURBO 是已验证
的 12 步 EasyCache 路线，目前更稳定。

## 7. 首帧生成

1. 连接一张图片作为首帧。
2. 描述人物动作、镜头运动、环境变化和声音。
3. 点击生成。

提示词可按以下顺序写：

```text
主体保持：人物脸、发型、服装和场景不变。
动作：人物缓慢转身并抬起右手。
镜头：中景，镜头轻微横移，不切镜。
光线：保持首帧光线。
声音：室内环境声、衣料摩擦声；不需要音乐。
```

成功标志：视频从首帧自然开始，主体没有立即换脸，动作方向正确，并返回末帧。

## 8. 首尾帧生成

首帧决定从哪里开始，尾帧决定到哪里结束。两张图的角色、画幅、光线和机位越接近，
过渡越稳定。

适合：转身、推拉镜头、人物从 A 点走到 B 点、门开合、昼夜变化。

不适合：首尾帧人物完全不同、机位跨越过大、场景结构冲突。

成功标志：第一帧接近首图，最后一帧接近尾图，中间没有明显闪断。

## 9. 多参考图

参考图模式允许 1–9 张图片。建议按用途标记：

- `角色：人物名`
- `场景：场景名`
- `道具：道具名`
- `服装：服装名`
- `风格：风格名`

系统会把它们连续编号为 `<Picture 1>`、`<Picture 2>`。通常 2–4 张清晰且不冲突的
参考图比塞满 9 张更可靠。

## 10. 人物三视图的实际用法

先只用正面图跑 5 秒 TURBO，确认脸和服装；需要转身时再加入侧面/背面图。不要让三张
图的发型、服装、年龄或光线明显不同。

成功标志：人物转身前后仍像同一个人，服装主色和轮廓没有突然改变。

排查：若换脸，减少参考图、降低动作幅度、缩短镜头，并把“保持人物身份、发型、服装”
写在动作之前。

## 11. 镜头续拍

第一段完成后，把自动返回的末帧连接到下一段的首帧：

```text
Shot01 → 自动末帧 → Shot02 → 自动末帧 → Shot03
```

每段只安排一个主要动作。比方说，第一段“起身”，第二段“走到门边”，第三段“开门”。

成功标志：Shot02 第一帧与 Shot01 最后一帧连续，人物位置和朝向基本一致。

## 12. 声音与静音

- 需要 H3 同步声音时，提示词要写环境声、对白和动作声音。
- 不需要声音时，关闭“生成音频”；系统会输出纯视频，不保留空 AAC 音轨。
- 正式对白仍建议后期配音，因为生成对白的清晰度和口型不是每条都稳定。

成功标志：关闭音频时播放器没有音轨；开启时有声音，且关键动作附近出现对应声音。

## 13. 怎样提高出片率

- 一条镜头只写一个主要动作和一个主要运镜。
- 先用 5 秒，稳定后再尝试 8/10/15 秒。
- 参考图与目标画幅尽量一致。
- 人物手部是重点时，让手在首帧中清晰可见，不要突然从画外伸入。
- 需要对白时避免同时安排高速动作和复杂运镜。
- 同一镜头保留 3–4 个候选，再人工挑选，不要只生成一次。

## 14. 这套导演台的实用边界

适合：分镜预演、短镜头候选、首尾帧转场、角色/场景资产复用、本地隐私素材。

不适合：一次生成整集、绝对一致的人脸/手指/口型、多人高速打斗、直接输出最终 4K
成片。480P 结果通常还要经过补帧、放大、降噪和剪辑。

## 15. 当前为什么不接入社区 4 步 Turbo

在本机 RTX 4070 SUPER 上，相同 864×480、5 秒测试结果为：

- 现有 12 步 EasyCache：中位约 160 秒
- ModelTC 4 步：中位约 115 秒，快约 28%
- ModelTC 6 步：约 160 秒，没有收益
- ModelTC 8 步：约 205 秒，反而更慢
- Larry v4 6 步：约 173 秒，反而更慢

4 步没有达到预设的 35% 加速门槛，镜头轨迹也与基线有明显差异。因此目前继续使用
生产 `TURBO/FAST/QUALITY` 三档，不在界面增加实验档。

## 16. 查看服务状态

在服务器终端执行：

```bash
cd /data/DramaClaw
/usr/local/bin/docker-compose -p dramaclaw ps
curl -f http://127.0.0.1:8780/healthz
systemctl is-active minimax-h3-comfyui.service
curl -f http://127.0.0.1:18189/system_stats
```

成功标志：API 为 `healthy`，两个 curl 成功，systemd 显示 `active`。

## 17. 常见问题与排查

### 网页打不开

```bash
cd /data/DramaClaw
/usr/local/bin/docker-compose -p dramaclaw ps
```

确认 `api` 和 `web` 都是 `Up`。若 API 为 unhealthy，查看第 18 节日志。

### 模型列表没有 H3

先刷新网页，再检查 API 和 H3 状态。不要直接重装 H3，也不要覆盖服务器 `.env`。

### 提示 H3 不可访问

```bash
systemctl status minimax-h3-comfyui.service --no-pager
curl -f http://127.0.0.1:18189/system_stats
```

### 显存不足

等待其他 H3、ComfyUI 或 Llama 任务结束，使用 480P、5 秒、TURBO 重试。不要同时在
ComfyUI 页面手动运行另一个大工作流。

### 任务一直排队

单张 12GB 显卡默认只运行一个 H3 任务。前一个任务完成后会自动继续。若长时间没有
变化，先查看 H3 日志，不要重复点击生成制造更多队列。

### 参考图被拒绝

确认数量为 1–9 张，且参考输入中没有视频或音频。检查图片文件是否损坏。

### 生成成功但画面不理想

这不是服务故障。先减小动作和运镜、减少冲突参考图、缩短到 5 秒，再生成 3 个候选。

## 18. 查看日志

DramaClaw：

```bash
cd /data/DramaClaw
/usr/local/bin/docker-compose -p dramaclaw logs --tail=100 api web
```

H3：

```bash
journalctl -u minimax-h3-comfyui.service -n 100 --no-pager
```

成功标志：没有连续重复的 OOM、Traceback 或容器重启记录。

## 19. 安全重启

只重启导演台：

```bash
cd /data/DramaClaw
/usr/local/bin/docker-compose -p dramaclaw restart
```

只重启 H3：

```bash
sudo systemctl restart minimax-h3-comfyui.service
```

重启 H3 前确认没有生成中的任务。成功标志：导演台 API 为 `healthy`，H3 为 `active`。

## 20. 更新与回滚注意事项

- 更新前备份 `.env` 和数据卷；`.env` 含隐私信息，不要发到群聊或提交 Git。
- 不要使用带 `-v` 的 `docker-compose down`，否则会删除数据卷。
- 不要在 H3 正在生成时升级 Docker、显卡驱动或 PyTorch。
- 当前 CUDA 13 优化需要更高版本的 NVIDIA 驱动；这是系统级维护，应另开停机窗口。

本次 v1.3.2 升级前备份：

```text
/data/backups/dramaclaw/20260815-pre-v132/
```
